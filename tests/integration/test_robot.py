
from unittest.mock import Mock

from src.robots import BaseRobot


def test_send_command_to_robot():
    raise NotImplementedError
    # map = Mock()
    # robot = BaseRobot(map)

    # message = robot.command("N")

    # expected_msg = Message()
    # expected_msg.start_position = BaseLocation()
    # expected_msg.current_position = BeseLocation(x=0, y=1)

    # assert_message(message, expected_msg) 

    # assert message.start_position == BaseLocation()
    # assert message.current_position == BeseLocation(x=0, y=1)
